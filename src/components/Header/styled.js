import styled from "styled-components"

export const TopoWapper = styled.header`
  position: fixed;
  top: 0;
  z-index: 9;
  width: 100%;
  background: rgba(27, 30, 38, 1);
  padding: 14px 0;
`

export const TopoCenter = styled.div`
  max-width: 1120px;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 20px;
`
